Run:

```bash
pip install -r reqs.txt
python manage.py makemigrations # first time setup
python manage.py migrate # first time setup
python manage.py runserver # diretso na dito pag na-setup na local DB
```
